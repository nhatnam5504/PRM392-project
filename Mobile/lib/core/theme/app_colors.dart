import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary — Cyan-600
  static const primary = Color(0xFF0891B2);
  static const primaryLight = Color(0xFFE0F2FE);
  static const primaryDark = Color(0xFF0E7490);
  static const primaryShadow = Color(0x4D0093A3);

  // Accent — Orange-500
  static const accent = Color(0xFFF97316);
  static const accentLight = Color(0xFFFFEDD5);
  static const accentShadow = Color(0x4DFF6B35);

  // Backgrounds
  static const background = Color(0xFFFAFAFA);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceDark = Color(0xFFF1F5F9);
  static const inputBackground = Color(0xFFF8FAFC);

  // Text
  static const textHeading = Color(0xFF0F172A);
  static const textPrimary = Color(0xFF1E293B);
  static const textSecondary = Color(0xFF64748B);
  static const textHint = Color(0xFF94A3B8);
  static const textBrand = Color(0xFF94A3B8);

  // Borders & Dividers
  static const border = Color(0xFFE2E8F0);
  static const borderLight = Color(0xFFF3F4F6);
  static const divider = Color(0xFFF1F5F9);

  // Semantic
  static const success = Color(0xFF16A34A);
  static const error = Color(0xFFF87171);
  static const warning = Color(0xFFF97316);
  static const star = Color(0xFFEAB308);
  static const outOfStock = Color(0xFF9CA3AF);

  // Navigation
  static const navActive = Color(0xFF0891B2);
  static const navInactive = Color(0xFF94A3B8);
}
